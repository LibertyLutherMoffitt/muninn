[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "muninn"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "PyNaCl",
  "dbus-python",
]

[project.scripts]
muninn = "muninn.cli:main"
